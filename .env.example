# Supabase Configuration for Static Deployment
# NOTE: For static deployment, only frontend variables are needed
# Backend server is no longer required as we use Supabase Edge Functions

VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# IMPORTANT: For Edge Functions to work properly, you need to set the following secrets in your Supabase dashboard:
# - JWT_SECRET (same as this one)
# - CRONJOB_SECRET (same as this one)
# - SERVICE_ROLE_KEY (instead of SUPABASE_SERVICE_ROLE_KEY)
# - EMAIL_HOST (if using email features)
# - EMAIL_PORT (if using email features)
# - EMAIL_USER (if using email features)
# - EMAIL_PASSWORD (if using email features)

JWT_SECRET=your_jwt_secret_here
CRONJOB_SECRET=your_cronjob_secret_here
SERVICE_ROLE_KEY=your_service_role_key

# Frontend URL (for CORS)
FRONTEND_URL=https://idcashier.my.id

# Duitku Configuration
# Duitku Signature Algorithm (md5 or sha-256)
DUITKU_SIGNATURE_ALGO=sha-256

# Site URL for password reset redirects
VITE_SITE_URL=https://idcashier.my.id

# Email Configuration (optional, for password reset)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your_email@gmail.com
# EMAIL_PASSWORD=your_app_password

# After setting up environment variables, you can run the application in static mode:
# npm run dev (frontend only, no backend server needed)

# For complete static deployment instructions, see:
# - STATIC_DEPLOYMENT_GUIDE.md (Indonesian)
# - SUPABASE_DEPLOYMENT_INSTRUCTIONS.md (English)
