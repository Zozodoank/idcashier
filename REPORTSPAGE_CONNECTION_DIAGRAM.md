# 🔄 ReportsPage Backend Connection - Visual Diagram

## ARSITEKTUR BACKEND COMPARISON

### **1. DashboardPage (Menggunakan Edge Functions)**

```
┌──────────────────────────────────────────────────────────────────┐
│                       DashboardPage.jsx                          │
│                  (Dashboard halaman utama)                       │
└────────────────┬─────────────────┬─────────────────┬─────────────┘
                 │                 │                 │
                 │                 │                 │
         ┌───────▼────────┐ ┌──────▼───────┐ ┌─────▼──────────┐
         │  dashboard-    │ │  dashboard-  │ │  dashboard-    │
         │     stats      │ │ top-products │ │   recent-      │
         │  Edge Function │ │Edge Function │ │ transactions   │
         │   (Deno)       │ │   (Deno)     │ │ Edge Function  │
         └───────┬────────┘ └──────┬───────┘ └─────┬──────────┘
                 │                 │                 │
                 └────────┬────────┴────────┬────────┘
                          │                 │
                  ┌───────▼─────────────────▼────────┐
                  │   Supabase PostgreSQL Database   │
                  │  (dengan RLS Policies active)    │
                  └──────────────────────────────────┘

✅ Pros: Konsisten, mudah monitoring, scalable
❌ Cons: Latency lebih tinggi, perlu deploy, lebih kompleks
```

### **2. ReportsPage (Menggunakan Direct Client) - CURRENT**

```
┌──────────────────────────────────────────────────────────────────┐
│                       ReportsPage.jsx                            │
│                   (Reports & Analytics)                          │
└────────────┬──────────────┬──────────────┬──────────────────────┘
             │              │              │
    Line 240 │     Line 257 │     Line 262 │
             │              │              │
      ┌──────▼──────┐ ┌─────▼──────┐ ┌────▼────────┐
      │ productsAPI │ │suppliersAPI│ │  salesAPI   │
      │   .getAll() │ │  .getAll() │ │  .getAll()  │
      └──────┬──────┘ └─────┬──────┘ └────┬────────┘
             │              │              │
             │  src/lib/api.js              │
             │  Direct Supabase Client      │
             │  (lines 481, 1446, 716)     │
             │              │              │
             └──────┬───────┴──────┬───────┘
                    │              │
            ┌───────▼──────────────▼────────┐
            │   Supabase Client Library     │
            │  (RLS policies diterapkan)    │
            └───────────────┬───────────────┘
                            │
                    ┌───────▼─────────────────┐
                    │ Supabase PostgreSQL DB  │
                    │ (dengan RLS Policies)   │
                    └─────────────────────────┘

✅ Pros: Sederhana, cepat, latency rendah, no deploy
❌ Cons: Tidak konsisten dengan Dashboard, monitoring sulit
```

---

## DATA FLOW DETAIL - ReportsPage

### **Step 1: Component Mount & Authentication**

```
┌─────────────────────────────────────────────────────────────┐
│  ReportsPage.jsx - useEffect() line 423                     │
│  Triggered when: component mounts, user/token changes       │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │  Check Auth:  │
                    │  user? ✓      │
                    │  token? ✓     │
                    └───────┬───────┘
                            │
                            ▼ Yes, proceed
                    ┌───────────────┐
                    │  fetchData()  │
                    │  line 209     │
                    └───────┬───────┘
                            │
                            ▼
```

### **Step 2: Parallel Data Fetching**

```
                    ┌───────────────────────┐
                    │   fetchData()         │
                    │   setIsLoading(true)  │
                    └───────┬───────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌───────────────┐   ┌───────────────┐   ┌──────────────┐
│ productsAPI   │   │ suppliersAPI  │   │  salesAPI    │
│  .getAll()    │   │  .getAll()    │   │  .getAll()   │
│               │   │               │   │              │
│ SELECT *      │   │ SELECT *      │   │ SELECT *,    │
│ FROM products │   │ FROM suppliers│   │ sale_items() │
│               │   │               │   │ FROM sales   │
└───────┬───────┘   └───────┬───────┘   └──────┬───────┘
        │                   │                   │
        ▼                   ▼                   ▼
   productsData        suppliersData        salesData
   (1 record)          (1 record)           (3 records)
```

### **Step 3: Data Transformation**

```
┌─────────────────────────────────────────────────────────────┐
│  Build productsMap (lines 243-254)                          │
│  {                                                           │
│    [product_id]: {                                           │
│      name: "SOSIS",                                          │
│      cost: 1500,                                             │
│      supplier_name: "PT. BAROKAH"                            │
│    }                                                         │
│  }                                                           │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  Transform salesData (lines 267-354)                        │
│  For each sale:                                             │
│    For each sale_item:                                      │
│      - Get product name from sale_item                      │
│      - Get cost from productsMap[product_id]                │
│      - Get supplier from productsMap[product_id]            │
│      - Calculate itemSubtotal = qty × price                 │
│      - Add flags: hasUnknownProduct, hasNegativeTotal, etc  │
│                                                             │
│  Result: transformedData[] - One row per item               │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  transformedData = [                                        │
│    {                                                        │
│      id: "sale1-0",            // Unique row ID             │
│      saleId: "sale1",          // Original sale ID          │
│      date: "2025-10-22",                                    │
│      product: "SOSIS",                                      │
│      customer: "Unknown Customer",                          │
│      supplier: "PT. BAROKAH",  // From productsMap          │
│      quantity: 1,                                           │
│      price: 2000,                                           │
│      itemSubtotal: 2000,       // qty × price               │
│      total: 2000,              // Sale total                │
│      cost: 1500,               // From productsMap          │
│      isFirstItemInSale: true   // Show totals once          │
│    },                                                       │
│    { ... sale2 items ... },                                 │
│    { ... sale3 items ... }                                  │
│  ]                                                          │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
```

### **Step 4: State Update**

```
┌─────────────────────────────────────────────────────────────┐
│  setAllSalesData(transformedData)     // Original data      │
│  setFilteredData(transformedData)     // Currently visible  │
│  setProducts([...])                   // For filter dropdown│
│  setCustomers([...])                  // For filter dropdown│
│  setSuppliers([...])                  // For filter dropdown│
│  setIsLoading(false)                  // Hide loader        │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │  Data Ready!  │
                    │  Render tabs  │
                    └───────────────┘
```

---

## TAB RENDERING FLOW

### **All Tabs Read from Same State: `filteredData`**

```
                    ┌──────────────────────────┐
                    │   filteredData State     │
                    │  (3 items dari 3 sales)  │
                    └────────┬─────────────────┘
                             │
         ┌───────────────────┼───────────────────┐
         │                   │                   │
         ▼                   ▼                   ▼
┌────────────────┐  ┌────────────────┐  ┌───────────────┐
│ Tab: Overview  │  │Tab: Profit/Loss│  │Tab: Trans.    │
│ (lines 695-757)│  │(lines 759-978) │  │(lines 981-1099│
├────────────────┤  ├────────────────┤  ├───────────────┤
│ Chart:         │  │ Stats Cards:   │  │ Table:        │
│ profitLossData │  │ - Total Rev    │  │ All items     │
│ (computed from │  │ - Total Cost   │  │ with columns: │
│  filteredData) │  │ - Total Profit │  │ - Date        │
│                │  │ - Profit %     │  │ - Product     │
│ Cards:         │  │                │  │ - Customer    │
│ - Total Rev    │  │ Table:         │  │ - Supplier    │
│   Rp 10,000    │  │ Per-item with  │  │ - Cashier     │
│ - Total Trans  │  │ profit calc    │  │ - Qty, Price  │
│   3 items      │  │                │  │ - Total       │
│ - Avg Trans    │  │ Chart:         │  │               │
│   Rp 3,333     │  │ Line chart     │  │ Features:     │
│                │  │ Revenue/Cost/  │  │ - Select all  │
│                │  │ Profit trend   │  │ - Bulk delete │
│                │  │                │  │ - Print       │
└────────────────┘  └────────────────┘  └───────────────┘
        ✅                  ✅                  ✅
   Displays data      Displays data      Displays data
```

**CRITICAL INSIGHT:** 
Semua tab menggunakan `filteredData` yang sama, jadi:
- ✅ Jika Overview menampilkan data → Semua tab menampilkan data
- ✅ Filter berlaku untuk semua tab sekaligus
- ✅ Tidak mungkin satu tab OK tapi tab lain kosong

---

## EDGE FUNCTIONS STATUS

### **Edge Functions yang Ada (Tapi Tidak Digunakan):**

```
supabase/functions/
├── sales-get-all/
│   └── index.ts              ❌ NOT CALLED by ReportsPage
│       ├── Lines 25-30: Simplified token handling
│       └── Line 39-46: Query sales with items
│
├── products-get-all/
│   └── index.ts              ❌ NOT CALLED by ReportsPage
│       ├── Lines 25-30: Simplified token handling
│       └── Line 38-43: Query products
│
└── suppliers-get-all/
    └── index.ts              ❌ NOT CALLED by ReportsPage
        ├── Lines 25-30: Simplified token handling
        └── Line 38-43: Query suppliers
```

### **Mengapa Tidak Digunakan?**

**Design Decision:** ReportsPage sengaja menggunakan Direct Client

**Reasoning:**
```
✅ Simplicity    - Tidak perlu deploy edge functions
✅ Performance   - Direct connection lebih cepat
✅ Maintenance   - Lebih sedikit moving parts
✅ RLS Policies  - Sudah handle security dengan baik
```

---

## FILTER MECHANISM

### **Filter Flow:**

```
┌────────────────────────────────────────────────────┐
│  User Actions:                                     │
│  1. Select date range: 20 Oct - 26 Oct            │
│  2. Select product: "SOSIS"                        │
│  3. Select customer: "All Customers"               │
│  4. Select supplier: "PT. BAROKAH"                 │
│  5. Click "Apply Filters" button                   │
└────────────────────┬───────────────────────────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │  applyFilters()        │
        │  (lines 427-443)       │
        └────────┬───────────────┘
                 │
                 ▼
    Start with: allSalesData (original unfiltered)
                 │
                 ├─► Filter by date range
                 │   (if specified)
                 │
                 ├─► Filter by product
                 │   (if not "All Products")
                 │
                 ├─► Filter by customer
                 │   (if not "All Customers")
                 │
                 ├─► Filter by supplier
                 │   (if not "All Suppliers")
                 │
                 ├─► Filter by corrupt data
                 │   (if hideCorruptData enabled)
                 │
                 ▼
    setFilteredData(filtered)  // Update displayed data
                 │
                 ▼
    ┌────────────────────────┐
    │  All tabs re-render    │
    │  with filtered data    │
    └────────────────────────┘
```

---

## ERROR HANDLING & RETRY

### **Retry Mechanism:**

```
┌──────────────────────────┐
│  fetchData(attempt = 0)  │
└────────┬─────────────────┘
         │
         ▼
    ┌────────────┐
    │  Try fetch │
    └────┬───┬───┘
         │   │
    ✅   │   │   ❌ Error
         │   └────────────┐
         │                │
         ▼                ▼
    ┌─────────┐    ┌──────────────┐
    │ Success │    │ Check attempt│
    └─────────┘    └──────┬───────┘
                          │
                     attempt < 3?
                          │
                ┌─────────┴─────────┐
               Yes                 No
                │                   │
                ▼                   ▼
    ┌──────────────────┐  ┌────────────────┐
    │ Wait 2^attempt   │  │ Give up        │
    │ seconds          │  │ Show error     │
    │ (1s, 2s, 4s)     │  │ Empty data     │
    └────────┬─────────┘  └────────────────┘
             │
             ▼
    fetchData(attempt + 1)
```

**Retry Schedule:**
- Attempt 1: Wait 1 second
- Attempt 2: Wait 2 seconds
- Attempt 3: Wait 4 seconds
- After 3 fails: Show error, display empty data

---

## SECURITY: RLS POLICIES

### **How RLS Works:**

```
┌──────────────────────────────────────────────────┐
│  User Query: SELECT * FROM sales                 │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌────────────────────────────────────────────────────┐
│  Supabase applies RLS Policy:                      │
│  "Users can view tenant sales"                     │
│                                                    │
│  WHERE (user_id = auth.uid())                      │
│     OR (user_id IN (                               │
│         SELECT id FROM users                       │
│         WHERE tenant_id = auth.uid()               │
│     ))                                             │
└────────────────────┬───────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
    Owner/Admin              Cashier
        │                         │
        ▼                         ▼
┌─────────────────┐      ┌────────────────┐
│ Returns:        │      │ Returns:       │
│ - Own sales     │      │ - Own sales    │
│ - Cashier sales │      │   only         │
└─────────────────┘      └────────────────┘
```

---

## TROUBLESHOOTING FLOWCHART

```
┌──────────────────────────┐
│  Data tidak muncul?      │
└────────┬─────────────────┘
         │
         ▼
    ┌─────────────┐
    │ Check Auth  │  → Token valid? → No → Logout & login
    └──────┬──────┘                  → Yes ↓
           │
           ▼
    ┌──────────────────┐
    │ Check Console    │  → Errors? → Yes → Check error message
    │ (F12)            │            → No ↓
    └──────┬───────────┘
           │
           ▼
    ┌──────────────────┐
    │ Check Network    │  → Status 200? → No → Check Supabase
    │ Tab              │                → Yes ↓
    └──────┬───────────┘
           │
           ▼
    ┌──────────────────┐
    │ Check React State│  → filteredData → Empty → Check RLS
    │ (DevTools)       │   populated?    → Yes ↓
    └──────┬───────────┘
           │
           ▼
    ┌──────────────────┐
    │ Should work! ✅  │
    └──────────────────┘
```

---

**Diagram Created:** 22 Oktober 2025  
**For:** ReportsPage Backend Connection Analysis  
**Status:** ✅ COMPLETE

